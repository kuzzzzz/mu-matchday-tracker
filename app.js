/* App lives in index.html (single-file companion).
   This file is kept so older loader refs do not 404. */
console.info('mu-matchday-tracker: open index.html — the full app is self-contained there.');
