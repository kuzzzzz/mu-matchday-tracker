/* Full companion currently loaded from last good commit via index.html loader.
   My Season personal prediction history is prepared and will land in the next full push. */
console.info('mu-matchday-tracker loader active');
